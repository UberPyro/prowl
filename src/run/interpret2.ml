open Batteries
open Util

open State

module V = Value
module T = Type
module M = Module
module C = Capture
module S = State

module Run (E : Eval.S) = struct

  open E

  (* let rec program (_, e1) = e e1 *)

end
