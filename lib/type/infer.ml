open! Batteries

open Type
open Ast

(* let rec check_program env prog = List.map (check_component env) prog

and check_component env (comp, _) = match comp with
  | Def (s, e) -> infer env e *)

(* factor out code for literals *)
(* let infer env' = fold_expr env' {
  id = fun env (_, i, o) _ -> 
} *)
